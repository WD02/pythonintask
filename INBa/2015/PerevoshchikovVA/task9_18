# config: utf8
# Задание 9. Вариант 18.
# Текст задачи: Компьютер сообщает игроку, сколько букв в слове, и дает пять попыток узнать, есть ли какая-либо буква в слове, причем программа может отвечать только "Да" и "Нет". Вслед за тем игрок должен попробовать отгадать слово.
# F I.O. Перевощиков Владислав Андреевич
# DATE_LAST_UPDATE 01/06/16
import random
slova=("Компьютер", "сообщение", "буква", "игрок", "узнать", "попытки")
word=random.choice(slova)
pods=5
bukva=""
slovo=""
(chislo)=len(word)
print("В этом слове",str(chislo)," букв. Угадайте слово.")
print("У тебя " + str(pods) + " попыток угадать буквы")
while pods>0:
		print (word)# для проверки
		bukva=input("Введите букву\n")
		if bukva in list (word) :
			print("Да.")
			pods= pods - 1
		else:
			print("Нет.")
			pods= pods - 1
if pods==0:
		slovo=input("Ваши попытки кончились, введите слово.\n")
if slovo==word:
			print("Вы угадали слово.")
else:
			print("Вы не угадали слово.")
input("Нажмите Enter для выхода")
